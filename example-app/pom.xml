<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>net.norskel</groupId>
        <artifactId>auth-module-parent</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>
    <artifactId>auth-module-example-app</artifactId>
    <name>Auth Module - Example App</name>
    <description>
        Runnable example of consuming the auth-module extension. Also acts as a
        smoke test that the extension works from a real application, which the
        extension's own deployment tests cannot prove.
    </description>

    <properties>
        <!-- Sample application: never published to the registry. -->
        <maven.deploy.skip>true</maven.deploy.skip>
    </properties>

    <repositories>
        <repository>
            <id>github-norskel-auth</id>
            <url>https://maven.pkg.github.com/Norskel/Norskel-Auth-Module</url>
        </repository>
    </repositories>

    <dependencies>
        <dependency>
            <groupId>net.norskel</groupId>
            <artifactId>auth-module</artifactId>
            <version>0.0.5</version>
        </dependency>

        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-junit5</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.rest-assured</groupId>
            <artifactId>rest-assured</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- KeycloakTestClient: mints real access tokens against the Keycloak
             that Dev Services starts, so the OIDC path can be tested. -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-test-keycloak-server</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>io.quarkus</groupId>
                <artifactId>quarkus-maven-plugin</artifactId>
                <extensions>true</extensions>
                <executions>
                    <execution>
                        <goals>
                            <goal>build</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
